@model FSP.AttendanceClock.Core.Entities.User

@{
    ViewData["Title"] = "Restablecer Contraseña";
}

<div class="card glass-effect" style="max-width: 500px; margin: auto;">
    <div class="card-header">
        <h2>Restablecer Contraseña para @Model.Username</h2>
    </div>
    <div class="card-body">
        <form asp-action="ResetPassword" method="post">
            <input type="hidden" name="id" value="@Model.Id" />
            <div class="mb-3">
                <label for="newPassword" class="form-label">Nueva Contraseña</label>
                <input type="text" name="newPassword" class="form-control" required />
                <small class="text-muted">Introduce la nueva contraseña para el empleado.</small>
            </div>
            
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-warning">Restablecer Contraseña</button>
                <a asp-action="Users" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>
