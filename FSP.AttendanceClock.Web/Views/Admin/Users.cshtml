@model IEnumerable<FSP.AttendanceClock.Core.Entities.User>

@{
    ViewData["Title"] = "User Management";
}

<div class="dashboard-container" style="max-width: 1000px;">
    <div class="header glass-effect mb-4 p-4 rounded d-flex justify-content-between align-items-center">
        <div>
            <h1>Users</h1>
            <p class="text-muted">Manage employees and administrators</p>
        </div>
        <div>
            <a asp-action="CreateUser" class="btn btn-success">New User</a>
            <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
        </div>
    </div>

    <div class="glass-effect p-4 rounded">
        @if (!Model.Any())
        {
            <div class="text-center py-5 text-muted">
                <i class="bi bi-people fs-1 mb-3 d-block"></i>
                <p>No users created yet.</p>
                <a asp-action="CreateUser" class="btn btn-success btn-sm">Create first user</a>
            </div>
        }
        else
        {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.Username</td>
                            <td>
                                @if (user.Role == FSP.AttendanceClock.Core.Entities.UserRole.Administrador)
                                {
                                    <span class="badge bg-success">Administrator</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Employee</span>
                                }
                            </td>
                            <td>
                                <div class="dropdown">
                                    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Actions
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li>
                                            <a asp-action="ResetPassword" asp-route-id="@user.Id" class="dropdown-item">
                                                <i class="bi bi-key"></i> Reset password
                                            </a>
                                        </li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li>
                                            <form asp-action="DeleteUser" method="post" class="d-inline">
                                                <input type="hidden" name="id" value="@user.Id" />
                                                <button type="submit" class="dropdown-item text-danger" data-confirm="Are you sure you want to delete this user? This action cannot be undone.">
                                                    <i class="bi bi-trash"></i> Delete
                                                </button>
                                            </form>
                                        </li>
                                    </ul>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>

@section Scripts {
    <script>
        document.querySelectorAll('[data-confirm]').forEach(function (btn) {
            btn.addEventListener('click', function (e) {
                if (!confirm(this.dataset.confirm)) {
                    e.preventDefault();
                }
            });
        });
    </script>
}
